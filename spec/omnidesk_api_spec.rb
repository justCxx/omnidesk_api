require 'spec_helper'

describe OmnideskApi do
  it 'has a version number' do
    expect(OmnideskApi::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
